import React from 'react'

const card = (props) => {
  return (
    <div>

    </div>
  )
}

const SmallCard = styled.div`
  border-radius: 12px;
  box-shadow: 0 4px 6px 0 rgba(0, 0, 0, 0.07);
`

export default card
